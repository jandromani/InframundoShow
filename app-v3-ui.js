function renderKpis(){
 const x=world?.global||{};const vals={riskKpi:x.risk,shipKpi:x.shipping_stress,energyKpi:x.energy_stress,tradeKpi:x.trade_stress,politicalKpi:x.political_stress};Object.entries(vals).forEach(([id,v])=>{const e=$('#'+id);if(e)e.textContent=Math.round(v||0)+'%';const bar=e?.parentElement?.querySelector('i');if(bar)bar.style.setProperty('--value',clamp(v||0)+'%')});$('#memoryKpi').textContent=(world?.history||[]).length;$('#updatedChip').textContent=(mode==='live'?'LIVE ':'FORK ')+fmt(world?.updated_at||sim?.sim_date);$('#modeText').textContent=mode==='live'?'LIVE WORLD':`FORK · ${C(player).flag} ${C(player).name}`;const brains=Object.keys(world?.country_brains||{}).length;$('#aiActing').textContent=`AI NATIONS ACTING ${brains||Object.keys(world?.countries||{}).length}`;$('#simClock').textContent=mode==='live'?'live sensors + autonomous brains':autoplay?'autonomous turns running':'autonomous fork paused';const tc=$('#turnCounter'),td=$('#turnDate');if(tc)tc.textContent=mode==='live'?'LIVE':`TURN ${sim?.turn||0}`;if(td)td.textContent=mode==='live'?String(world?.updated_at||'').slice(0,10):(sim?.sim_date||'—');
}
function bars(obj,labels){return Object.entries(labels).map(([k,label])=>{const v=Math.round(obj?.[k]||0);return`<div class="vector"><div class="vectorhead"><span>${esc(label)}</span><b>${v}</b></div><div class="track"><div class="fill" style="width:${clamp(v)}%"></div></div></div>`}).join('')}
function renderFront(){ensureSelected();const p=pair(selectedId);if(!p)return;const a=C(p.a),b=C(p.b);$('#frontTitle').textContent=`${a.flag} ${a.name} ↔ ${b.flag} ${b.name}`;$('#frontScore').textContent=Math.round(p.score);$('#frontScore').style.color=scoreColor(p.score);const tr=Number(p.trend||0);$('#frontTrend').className='small trend '+(tr>0?'up':tr<0?'down':'');$('#frontTrend').textContent=`${tr>0?'▲ +':tr<0?'▼ ':'→ '}${tr} · ${p.scan_articles||0} fresh signals · GW ${Math.round(p.gearwatch_pressure||0)}`;$('#frontSummary').textContent=p.summary||'No summary.';$('#frontDomains').innerHTML=(p.domains||p.domain||[]).map(x=>`<span>${esc(x)}</span>`).join('');$('#vectors').innerHTML=bars(p.vector,DIM_LABELS_VECTOR);$('#restraints').innerHTML=bars(p.restraints,REST_LABELS);$('#drivers').innerHTML=(p.drivers||[]).map(x=>`<div class="driver">${esc(x)}</div>`).join('');$('#botMoves').innerHTML=(p.bot_moves||[]).map(x=>`<div>⚙ ${esc(x)}</div>`).join('')||'<div>No strategic move stored.</div>';$('#news').innerHTML=(p.news||[]).slice(0,7).map(n=>`<a href="${safeUrl(n.url)}" target="_blank" rel="noopener"><b>${esc(n.title)}</b><br><span>${esc(n.source||'')} · ${esc(String(n.seen||'').slice(0,10))}</span></a>`).join('')||'<div>No fresh article attached.</div>';$('#pairGear').innerHTML=(p.gearwatch_impacts||[]).map(x=>`<span class="pill">⚙ ${esc(x.label)} ${Math.round(x.score)}</span>`).join('')||'<span class="small muted">No direct GearWatch mechanism mapped.</span>';renderActions(p)}
function renderRanking(){const rows=(world?.pairs||[]).slice().sort((a,b)=>val(b)-val(a)).slice(0,18);$('#ranking').innerHTML=rows.map((p,i)=>`<div class="rankrow" data-id="${p.id}"><b>#${i+1}</b><div><div>${C(p.a).flag} ${esc(C(p.a).name)} ↔ ${C(p.b).flag} ${esc(C(p.b).name)}</div><div class="rankbar"><i style="width:${clamp(val(p))}%"></i></div></div><b style="color:${scoreColor(val(p))}">${Math.round(val(p))}</b></div>`).join('');$$('.rankrow').forEach(el=>el.onclick=()=>{selectedId=el.dataset.id;const p=pair(selectedId);if(p)focusCountry=p.a;highlightPairs=[];render()})}
function postureClass(p=''){p=String(p).toLowerCase();if(/crisis|assert|deter|escal/.test(p))return'escalate';if(/hedge|opportun|rerout/.test(p))return'hedge';return'cooperate'}
function renderAgentRoster(){const brains=world?.country_brains||{};let rows=Object.entries(world?.countries||{}).filter(([code,c])=>brains[code]||c.fronts).sort((a,b)=>(b[1].max_front||0)-(a[1].max_front||0));const q=($('#agentSearch')?.value||'').toLowerCase().trim();if(q)rows=rows.filter(([code,c])=>(c.name+' '+code).toLowerCase().includes(q));rows=rows.slice(0,36);$('#agentRoster').innerHTML=rows.map(([code,c])=>{const brain=brains[code]||{};const posture=brain.posture||((c.max_front||0)>80?'DETER':(c.energy_dependency_risk||0)>25?'HEDGE':'BALANCED');const last=mode==='sim'&&sim?.last_moves?.[code]?.type;const shown=last||posture;const pc=postureClass(shown);return`<div class="agent-row ${code===focusCountry?'active':''}" data-code="${code}"><div class="agent-flag">${c.flag||'◌'}</div><div><div class="agent-name">${esc(c.name||code)}</div><div class="agent-meta">AI controlled · fronts ${c.fronts||0}</div><div class="microbars"><span class="microbar"><i style="width:${clamp(c.economy_live||c.economy||50)}%"></i></span><span class="microbar"><i style="width:${clamp(c.military||50)}%"></i></span><span class="microbar"><i style="width:${clamp(c.stability_live||c.stability||50)}%"></i></span></div></div><div class="agent-action ${pc}">${esc(shown.replaceAll('_',' '))}</div></div>`}).join('');$$('.agent-row').forEach(el=>el.onclick=()=>selectCountry(el.dataset.code))}
function selectedBrain(){return world?.country_brains?.[focusCountry]||{}}
function renderCountryCommand(){const c=C(focusCountry),b=selectedBrain();$('#selectedCountryFlag').textContent=c.flag||'◌';$('#selectedCountryName').textContent=c.name||focusCountry;$('#selectedCountryPosture').textContent=(b.posture||'BALANCED').replaceAll('_',' ');$('#selectedCountryStats').innerHTML=[['STABILITY',c.stability_live??c.stability],['ECONOMY',c.economy_live??c.economy],['MAX FRONT',c.max_front],['PRESSURE',c.pressure],['ENERGY SEC.',c.energy_security],['INFLUENCE',c.influence]].map(([k,v])=>`<span>${k}<b>${Math.round(Number(v)||0)}</b></span>`).join('');const goals=(b.goals||[]).slice(0,5);$('#selectedObjectives').innerHTML=goals.length?goals.map(g=>`<div class="objective">${esc((g.id||g.label||String(g)).replaceAll('_',' '))}</div>`).join(''):`<div class="objective">Preserve national stability</div><div class="objective">Protect critical dependencies</div><div class="objective">Manage hottest bilateral front</div>`;const deps=[];const fs=fronts(focusCountry).filter(p=>p.dependencies);for(const p of fs){const side=p.a===focusCountry?'a_on_b':'b_on_a',other=p.a===focusCountry?p.b:p.a;const trade=Number(p.dependencies?.trade?.[side]||0),energy=Number(p.dependencies?.energy?.[side]||0);if(trade>0)deps.push({label:`Trade · ${C(other).name}`,v:trade});if(energy>0)deps.push({label:`Energy · ${C(other).name}`,v:energy})}deps.sort((a,b)=>b.v-a.v);$('#selectedDependenciesMini').innerHTML=(deps.slice(0,5).map(d=>`<div class="dep-mini"><span>${esc(d.label)}</span><b>${d.v.toFixed(1)}%</b><i style="--pct:${clamp(d.v)}%"></i></div>`).join('')||'<div class="muted small">Dependency cache is still expanding.</div>');const rts=ROUTES.filter(r=>r.countries.includes(focusCountry)||['ESP','USA','CHN','MAR','DZA','TUR','IRN'].includes(focusCountry)).map(r=>({r,...routeAccess(r,focusCountry)})).sort((a,b)=>({blocked:2,contested:1,open:0}[b.status]-({blocked:2,contested:1,open:0}[a.status]))).slice(0,6);$('#selectedRoutes').innerHTML=rts.map(x=>`<div class="route-mini" data-route="${x.r.id}"><span>${esc(x.r.name)}</span><b class="${x.status}">${x.status.toUpperCase()}</b></div>`).join('');$$('.route-mini').forEach(e=>e.onclick=()=>inspectRoute(ROUTES.find(r=>r.id===e.dataset.route)));const riskRows=[['Trade exposure',c.trade_dependency_risk||Math.min(100,(c.pressure||0)*.8)],['Energy exposure',c.energy_dependency_risk||Math.min(100,(c.pressure||0)*.9)],['Conflict risk',c.max_front||0],['Political risk',c.political_event_pressure||0]];$('#countryRiskBars').innerHTML=riskRows.map(([k,v])=>`<div class="risk-row"><div><span>${k}</span><b>${Math.round(v)}</b></div><div class="risk-track"><i style="width:${clamp(v)}%"></i></div></div>`).join('');const moves=(b.next_moves||[]).slice(0,5);$('#selectedQueue').innerHTML=moves.length?moves.map((m,i)=>`<div class="move-item"><span class="move-num">${i+1}</span><span>${esc((m.type||'strategic move').replaceAll('_',' '))}${m.target?' → '+esc(C(m.target).name):''}<small class="muted">${m.reason?'<br>'+esc(m.reason):''}</small></span><span class="move-eta">+${2+i*2}d</span></div>`).join(''):'<div class="muted small">AI queue will populate on the next autonomous scan.</div>'}
function forecastTemplate(p){const A=C(p.a),B=C(p.b),dom=dominantDimension(p),rising=Number(p.trend||0)>0,score=Math.round(p.score||0);const map={military:rising?`${A.name} and ${B.name} may raise military readiness`:`${A.name} and ${B.name} could test a military confidence-building channel`,territorial:`Territorial pressure may reshape ${A.name}–${B.name} diplomacy`,energy:`Energy exposure may force a new ${A.name}–${B.name} bargaining round`,trade:`Trade dependence may trigger rerouting between ${A.name} and ${B.name}`,diplomatic:rising?`${A.name}–${B.name} diplomatic friction could intensify`:`${A.name} and ${B.name} may open a de-escalation channel`,domestic:`Domestic political pressure may harden ${A.name} policy toward ${B.name}`,information:`Information pressure may drive a new narrative contest between ${A.name} and ${B.name}`};return{title:map[dom]||`${A.name}–${B.name} relationship may shift`,dom,score}}
function renderForecastNews(){const generated=mode==='sim'?(sim?.generated_news||[]):[];const base=new Date(mode==='sim'?(sim?.sim_date||Date.now()):(world?.updated_at||Date.now()));const pairs=(world?.pairs||[]).slice().sort((a,b)=>(b.score+(b.trend||0)*2)-(a.score+(a.trend||0)*2)).slice(0,8);const cards=pairs.map((p,i)=>{const t=forecastTemplate(p),lead=Math.max(2,Math.round(25-(p.score||0)/4)+i),span=Math.max(3,Math.round(12-(p.score||0)/15));const d1=new Date(base);d1.setUTCDate(d1.getUTCDate()+lead);const d2=new Date(d1);d2.setUTCDate(d2.getUTCDate()+span);const sev=p.score>=82?'high':p.score>=65?'med':'low';return{p,t,sev,window:`${d1.toISOString().slice(0,10)} → ${d2.toISOString().slice(0,10)}`,body:`AI scenario · ${dominantDimension(p)} pressure ${Math.round(p.vector?.[dominantDimension(p)]||p.score)}/100 · trend ${Number(p.trend||0)>=0?'+':''}${Number(p.trend||0)} · GearWatch ${Math.round(p.gearwatch_pressure||0)}.`}});const actualGenerated=generated.slice(0,6).map(n=>({sev:n.severity||'med',window:n.date||sim?.sim_date,t:{title:n.headline},body:`AUTONOMOUS TURN EVENT · ${n.body||''}`,p:n.pair_id?pair(n.pair_id):null,generated:true}));const political=(world?.political_calendar?.events||world?.political_events||[]).filter(e=>e.date).slice(0,2).map(e=>({sev:'low',window:String(e.date).slice(0,10),t:{title:`Political event may change ${C(e.country||e.country_code).name} strategic utility`},body:esc(e.name||e.label||'Scheduled political event'),p:null}));const all=[...actualGenerated,...cards,...political].slice(0,12);$('#aiNewsFeed').innerHTML=all.map(x=>`<div class="ai-news-card ${x.sev} ${x.generated?'generated':''}" ${x.p?`data-id="${x.p.id}"`:''}><div class="news-time"><span>${esc(x.window)}</span><b class="news-severity ${x.sev}">${x.generated?'AI EVENT':x.sev.toUpperCase()}</b></div><h4>${esc(x.t.title)}</h4><p>${x.body}</p></div>`).join('');$$('.ai-news-card[data-id]').forEach(e=>e.onclick=()=>{selectedId=e.dataset.id;const p=pair(selectedId);if(p)focusCountry=p.a;render()});$('#forecastMeta').textContent=mode==='sim'?`${generated.length} autonomous events · dated AI scenarios from the forked world`:`${cards.length} dated AI scenarios generated from LIVE state · simulation outputs, not factual forecasts`}
function renderGear(){const gw=world?.gearwatch||{};$('#gearHeadline').textContent=gw.headline||'GearWatch causal radar';$('#gearBrief').textContent=gw.briefing||'';const arr=(gw.mechanisms||[]).filter(m=>m.impact_map).slice(0,12);$('#gearBoard').innerHTML=arr.map(m=>{const ids=m.impact_map?.pairs||[];const dims=Object.keys(m.impact_map?.domains||{}).join(' · ');return`<div class="gearitem" data-gw="${m.id}"><div class="row between"><b>${esc(m.label||m.id)}</b><span class="gscore">${Math.round(m.score||0)}</span></div><div class="small muted">${esc(m.state||'')} ${Number(m.delta)>0?'· ▲ '+m.delta:''}</div><div class="small">${esc(dims)}</div><div class="impactlist">${ids.slice(0,5).map(id=>`<span>${esc(id)}</span>`).join('')}</div></div>`}).join('')||'<div class="muted">No mapped causal mechanisms.</div>';$$('.gearitem').forEach(el=>el.onclick=()=>{const m=(gw.mechanisms||[]).find(x=>x.id===el.dataset.gw);if(!m)return;highlightPairs=m.impact_map?.pairs||[];const dims=Object.entries(m.impact_map?.domains||{}).sort((a,b)=>b[1]-a[1]);if(dims[0])setLayer(dims[0][0]);const hot=highlightPairs.map(id=>pair(id)).filter(Boolean).sort((a,b)=>b.score-a.score)[0];if(hot){selectedId=hot.id;focusCountry=hot.a}$('#causalPulse').innerHTML=`<b>${esc(m.label)}</b> · causal ${Math.round(m.score||0)}/100 · ${esc(m.state||'')}<br><span class="muted">Pressure mapped to ${(m.impact_map?.pairs||[]).join(', ')||'no bilateral fronts'}${(m.impact_map?.chokes||[]).length?' · chokepoints '+m.impact_map.chokes.join(', '):''}</span>`;render()})}
function renderChokes(){const arr=(world?.chokepoints||[]).slice().sort((a,b)=>b.score-a.score);$('#chokes').innerHTML=arr.map(c=>`<div class="choke" data-choke="${esc(c.id||c.name)}"><div class="row between"><b>${esc(c.name)}</b><strong style="color:${scoreColor(c.score)}">${Math.round(c.score)}</strong></div><div class="small muted">GW pressure ${Math.round((c.gearwatch_pressure||0)*10)/10}</div></div>`).join('')}
function renderCountries(){const arr=Object.entries(world?.countries||{}).sort((a,b)=>(b[1].max_front||0)-(a[1].max_front||0)).slice(0,18);$('#countries').innerHTML=arr.map(([code,c])=>`<div class="countrycard" data-code="${code}"><b>${c.flag} ${esc(c.name)}</b><div class="small muted">max front ${c.max_front||0} · avg ${c.pressure||0}</div><div class="small">stability ${c.stability_live||0} · economy ${c.economy_live||0}</div></div>`).join('');$$('.countrycard').forEach(el=>el.onclick=()=>selectCountry(el.dataset.code))}
function selectCountry(code){focusCountry=code;const fs=fronts(code);if(fs[0])selectedId=fs[0].id;render()}
function renderSpark(){const h=(world?.history||[]).slice(-72);const s=$('#spark');if(!h.length){s.innerHTML='';return}const w=600,hh=110;const vals=h.map(x=>Number(x.global_risk??0));const min=Math.min(...vals,0),max=Math.max(...vals,100);const pts=vals.map((v,i)=>[i/(Math.max(1,vals.length-1))*w,hh-((v-min)/(Math.max(1,max-min)))*(hh-12)-6]);const d=pts.map((p,i)=>(i?'L':'M')+p[0].toFixed(1)+','+p[1].toFixed(1)).join(' ');s.innerHTML=`<line x1="0" y1="55" x2="600" y2="55"></line><path d="${d}"></path>`;$('#historyLabel').textContent=`${h.length} snapshots · risk ${vals[0]||'—'} → ${vals.at(-1)||'—'}`}
function renderSim(){if(mode!=='sim')return;$('#simDate').textContent=sim.sim_date;$('#simLog').innerHTML=(sim.sim_log||[]).slice(0,14).map(x=>`<div>${esc(x)}</div>`).join('')}
function renderActions(p){if(mode!=='sim')return;const involved=p.a===player||p.b===player;const other=p.a===player?p.b:p.a;const acts=involved?[['talks','Open de-escalation channel','↓ diplomatic, ↑ mediation'],['pressure','Economic pressure','↑ trade + diplomatic pressure'],['readiness','Military readiness','↑ military + deterrence'],['energy','Energy shield','↓ energy vulnerability']]:[['mediate','Offer mediation','↓ diplomatic; ↑ mediation restraint']];$('#actions').innerHTML=`<div class="small muted">Acting as ${C(player).flag} ${C(player).name}${involved?' vs '+C(other).name:' on a third-party front'}.</div>`+acts.map(a=>`<button class="action" data-act="${a[0]}"><b>${a[1]}</b><div class="small muted">${a[2]}</div></button>`).join('');$$('.action[data-act]').forEach(b=>b.onclick=()=>applyAction(p.id,b.dataset.act))}
function applyAction(id,act){const p=pair(id);if(!p||mode!=='sim')return;let txt='';if(act==='talks'){p.vector.diplomatic=clamp(p.vector.diplomatic-12);p.vector.domestic=clamp(p.vector.domestic-2);p.restraints.alliance_mediation=clamp(p.restraints.alliance_mediation+6);txt=`${C(player).name} opens a de-escalation channel on ${id}.`}if(act==='pressure'){p.vector.trade=clamp(p.vector.trade+11);p.vector.diplomatic=clamp(p.vector.diplomatic+5);p.restraints.economic_interdependence=clamp(p.restraints.economic_interdependence-4);txt=`${C(player).name} applies economic pressure on ${id}.`}if(act==='readiness'){p.vector.military=clamp(p.vector.military+12);p.restraints.deterrence=clamp(p.restraints.deterrence+7);txt=`${C(player).name} raises military readiness on ${id}.`}if(act==='energy'){p.vector.energy=clamp(p.vector.energy-9);const c=world.countries[player];if(c)c.energy_security=clamp(c.energy_security+4);txt=`${C(player).name} launches an energy resilience package.`}if(act==='mediate'){p.vector.diplomatic=clamp(p.vector.diplomatic-6);p.restraints.alliance_mediation=clamp(p.restraints.alliance_mediation+8);txt=`${C(player).name} offers mediation on ${id}.`}const old=p.score;p.score=relationOverall(p);p.trend=p.score-old;sim.sim_log.unshift(txt);recalcSimulation();render()}
function rnd(seed){let x=seed>>>0;return()=>{x=(x*1664525+1013904223)>>>0;return x/4294967296}}
function resolveWeek(){if(mode!=='sim')return;const d=new Date(sim.sim_date+'T00:00:00Z');d.setUTCDate(d.getUTCDate()+7);sim.sim_date=d.toISOString().slice(0,10);sim.turn=(sim.turn||0)+1;const r=rnd(Number(sim.sim_date.replaceAll('-',''))+sim.turn*7919),events=[];sim.last_moves=sim.last_moves||{};sim.route_overrides=sim.route_overrides||{};const brains=world?.country_brains||{};const actors=Object.keys(brains).length?Object.keys(brains):Object.keys(world?.countries||{});for(const code of actors){const brain=brains[code]||{};const move=chooseAgentMove(code,brain,r);const evt=applyAgentMove(code,move,r);if(evt){events.push(evt);sim.last_moves[code]=evt}}for(const p of sim.pairs||[]){const old=p.score;const restraint=Object.values(p.restraints||{}).reduce((a,b)=>a+Number(b||0),0)/Math.max(1,Object.keys(p.restraints||{}).length);const noise=(r()-.5)*2.4-(restraint-55)*.004;const dim=dominantDimension(p);p.vector[dim]=clamp((p.vector[dim]||0)+noise);p.score=relationOverall(p);p.trend=Math.round((p.score-old)*10)/10}applyGearwatchTurnPressure(r,events);recalcSimulation();sim.generated_news=events.sort((a,b)=>(b.impact||0)-(a.impact||0)).slice(0,24);const hot=(sim.pairs||[]).slice().sort((a,b)=>b.score-a.score)[0];sim.sim_log.unshift(`TURN ${sim.turn} · ${sim.sim_date} · ${actors.length} country agents acted · ${events.length} visible strategic events · hottest ${hot?.id||'—'} ${Math.round(hot?.score||0)}/100.`);sim.sim_log=sim.sim_log.slice(0,60);render()}
function recalcSimulation(){const ps=world.pairs||[],scores=ps.map(p=>p.score);if(!scores.length)return;const avg=scores.reduce((a,b)=>a+b,0)/scores.length,hot=Math.max(...scores);world.global.risk=Math.round(clamp(avg*.58+hot*.42));const ch=world.chokepoints||[];world.global.shipping_stress=Math.round(ch.reduce((s,c)=>s+c.score,0)/Math.max(1,ch.length));const e=ch.filter(c=>c.domains?.includes('energy'));world.global.energy_stress=Math.round(e.reduce((s,c)=>s+c.score,0)/Math.max(1,e.length));const t=ch.filter(c=>c.domains?.includes('trade'));world.global.trade_stress=Math.round(t.reduce((s,c)=>s+c.score,0)/Math.max(1,t.length))}
function setMode(next){if(next==='live')stopAutoplay();mode=next;document.body.classList.toggle('sim',next==='sim');$('#liveBtn').classList.toggle('active',next==='live');$('#simBtn').classList.toggle('active',next==='sim');if(next==='sim'){if(!sim){sim=deep(live);sim.sim_date=(live.updated_at||new Date().toISOString()).slice(0,10);sim.sim_log=['Autonomous fork created from LIVE snapshot. Every active country remains AI-controlled; you can intervene without stopping the other agents.'];sim.turn=0;sim.generated_news=[];sim.route_overrides={};sim.last_moves={}}world=sim}else{world=live;sim=null}ensureSelected();render()}
function setLayer(next){layer=next;$$('.layer').forEach(b=>b.classList.toggle('active',b.dataset.layer===layer));$('#layerName').textContent=DIM_LABELS[layer]||layer;renderRanking();renderMap()}
function render(){renderKpis();renderFront();renderRanking();renderGear();renderChokes();renderCountries();renderAgentRoster();renderCountryCommand();renderForecastNews();renderSpark();renderSim();renderMap()}
function mapCountryValue(code){const fs=fronts(code);return fs.length?Math.max(...fs.map(p=>val(p))):0}
function inspectRoute(route){if(!route)return;selectedRoute=route.id;const access=routeAccess(route,focusCountry),global=routeGlobalStatus(route);const c=C(focusCountry);const gates=(route.gates||[]).map(x=>GATE_LABELS[x]||x).join(' · ')||'No critical chokepoint';$('#routeInspector').innerHTML=`<div class="route-inspector-head"><div><span class="mini-label">${esc(route.subtype)}</span><b>${esc(route.name)}</b></div><span class="status-badge ${access.status}">${access.status.toUpperCase()}</span></div><div class="route-inspector-body"><div><b>${c.flag} ${esc(c.name)} ACCESS</b> · ${access.status.toUpperCase()}</div><p>${esc(access.reason)}</p><div class="muted">Global route state: ${global.toUpperCase()}<br>Chokepoints: ${esc(gates)}<br><small>Modeled access, not a factual navigation instruction.</small></div></div>`;renderMap()}
function inspectChoke(c){if(!c)return;const routes=ROUTES.filter(r=>(r.gates||[]).some(g=>String(c.id||c.name).toUpperCase().includes(g)||String(c.name).toUpperCase().includes((GATE_LABELS[g]||g).replace('Strait of ','').toUpperCase())));const access=routes.map(r=>({r,...routeAccess(r,focusCountry)}));const weight={blocked:2,contested:1,open:0};const worst=(access.sort((a,b)=>weight[b.status]-weight[a.status])[0]?.status)||'contested';$('#routeInspector').innerHTML=`<div class="route-inspector-head"><div><span class="mini-label">STRATEGIC CHOKEPOINT</span><b>${esc(c.name)}</b></div><span class="status-badge ${worst}">${worst.toUpperCase()}</span></div><div class="route-inspector-body"><div><b>${C(focusCountry).flag} ${esc(C(focusCountry).name)} ACCESS</b></div><p>Global pressure ${Math.round(c.score||0)}/100 · GearWatch ${Math.round((c.gearwatch_pressure||0)*10)/10}.</p>${access.slice(0,4).map(x=>`<div class="route-mini"><span>${esc(x.r.name)}</span><b class="${x.status}">${x.status.toUpperCase()}</b></div>`).join('')}<small class="muted">Access is a game-model output and may differ by country.</small></div>`}
